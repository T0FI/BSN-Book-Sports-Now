<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group_project_2019_20_group_6">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".User_Analytics"></activity>
        <activity android:name=".Splash_screen2">

            <!-- Need to keep this as this so the splash screen works -->
            <!-- don't get rid of the intent-filter otherwise you'd get running app error -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Layout_Test" />
        <activity android:name=".LeagueMain" />
        <activity android:name=".UserProfile" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".Singlepayment" />
        <activity android:name=".Homepage" />
        <activity android:name=".Extra2Activity" />
        <activity android:name=".ExtraAdapter" />
        <activity android:name=".Extra1Activity" />
        <activity android:name=".Sports" />
        <activity android:name=".LeagueMainTest" />
        <activity android:name=".League_layout" />
        <activity android:name=".LeagueTest" />
        <activity android:name=".league_setup" />
        <activity android:name=".SplitPay" />
        <activity android:name=".Payment_choice" />
        <activity android:name=".Login_2" />
        <activity android:name=".SignUp" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>